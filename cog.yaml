# cog.yaml
build:
  gpu: true
  system_packages:
    - "ffmpeg"
    - "libmagic1"
    - "cudnn9-cuda-12"
  python_version: "3.10"
  python_requirements: requirements.txt
  run:
    - "echo env is ready!"
predict: "predict.py:Predictor"
